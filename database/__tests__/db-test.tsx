import * as db from '../db'

test('fetchQuetions Succeeds', () => {
    // Render a checkbox with label in the document
    //db.fetchGames().then(result => {
    //    console.log(result);
    //});
});